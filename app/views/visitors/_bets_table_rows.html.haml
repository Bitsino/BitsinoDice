- @bets.each do |bet|
  %tr
    %td.text-primary
      = bet.id
    %td
      = bet.user.name
    %td
      = time_ago_in_words(bet.created_at)
      ago.
    %td
      = "%f" % (bet.amount / 100000000.0).round(6)
    %td
      = bet.multiplier.round(2)
    %td
      - if bet.rolltype == 'under'
        <
      - else
        >
      = bet.game
    %td
      = bet.roll
    %td
      - if bet.win? or bet.amount == 0
        %span.label.label-success
          = "%f" % (bet.profit / 100000000.0)
      - else
        %span.label.label-default
          = "%f" % (-(bet.amount / 100000000.0))